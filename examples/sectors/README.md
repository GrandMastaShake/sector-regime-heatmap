# Sector Input Files

Each sector file is named in lowercase and contains a `sector` field plus `day`, `week`, and `month` component blocks. The assembler requires exactly 11 sector JSON files.

Inputs must be sourced and dated. Do not commit placeholder scores as a forecast artifact.